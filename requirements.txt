# Core RAG stack
langchain
langchain-community
langchain-openai
langchain-huggingface
langchain-chroma

# Vector store
chromadb

# Embeddings (local, no API key needed)
sentence-transformers

# LLM options
openai          # GPT-4o / GPT-4o-mini (paid)
langchain-groq  # Groq cloud — free tier, fast (recommended for eval)
langchain-ollama

# HuggingFace Tele-LLMs (LLM_PROVIDER=huggingface)
# Enables: AliMaatouk/Llama-3-8B-Instruct-Tele and other Tele-LLMs
transformers
accelerate

# Data handling
datasets     # HuggingFace datasets (TeleLogs, TeleTables, Tele-Eval, O-RAN Bench)
pandas
numpy
requests
pyzipper

# Document loaders
pypdf
unstructured

# Evaluation / metrics
ragas         # RAG evaluation framework (faithfulness, MRR, etc.)

# Utilities
tqdm
python-dotenv
rich         # pretty terminal output
tiktoken
pytest
streamlit    # web UI
